import { PageHero, SiteShell, ContactBand } from "../site-shell";
import { products } from "../site-data";
export const metadata = { title:"المنتجات والحلول | مؤسسة أسامة هيكل التجارية", description:"آلات ومعدات زراعية للبذار وتجهيز الأعلاف ومعالجة المخلفات والنقل الزراعي من مؤسسة أسامة هيكل التجارية." };
export default function Products(){return <SiteShell><main><PageHero eyebrow="المنتجات والحلول" title="معدات مصممة للعمل الحقيقي" copy="تصفح حلولنا في البذار، تجهيز الأعلاف، معالجة المخلفات الزراعية، الكبس والنقل. المواصفات المعروضة مستمدة حصراً من الملفات الفنية الموردة." />
<section className="section product-list"><div className="container">{products.map((p,i)=><article className="product-row" id={`p-${p.code}`} key={p.code}><div className="product-image"><img src={p.image.replace("-card.webp","-detail.webp")} alt={p.name} /><span>{p.code}</span></div><div className="product-body"><span className="eyebrow">حل زراعي {String(i+1).padStart(2,"0")}</span><h2>{p.name}</h2><h3>{p.tagline}</h3><p>{p.description}</p><div className="feature-chips">{p.features.map(f=><span key={f}>{f}</span>)}</div><details className="specs"><summary>عرض المواصفات الفنية <span>＋</span></summary><dl>{p.specs.map(([k,v])=><div key={k}><dt>{k}</dt><dd>{v}</dd></div>)}</dl>{p.note&&<p className="spec-note">ملاحظة: {p.note}</p>}</details></div></article>)}</div></section><ContactBand /></main></SiteShell>}
